
Iterate an array (FOR LOOP & FOR EACH)


Iterate an array using a FOR LOOP

  int[] nums = { 1, 2, 3}; // init array

  for(int i=0; i < nums.Length; i++)
  {
    System.Diagnostics.Debug.WriteLine("Elem " + i + ": " + nums[i]);
  }


    
Iterate an array using a FOR EACH

  int[] nums = { 1, 2, 3}; // init array

  foreach(int num in nums)
  {
    System.Diagnostics.Debug.WriteLine("Elem " + num);
  }